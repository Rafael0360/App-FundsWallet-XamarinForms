<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="FundsWalletAPP.Views.NewItemPage"
             Title="New Stock">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Name" FontSize="Large" />
            <Entry Text="{Binding Item.Name}" d:Text="Name" FontSize="Medium"  Margin="0"/>
            <Label Text="Date" FontSize="Large" />
            <Editor Text="{Binding Item.Date}" d:Text="Date" FontSize="Medium" Margin="0" />
            <Label Text="Price" FontSize="Large" />
            <Editor Text="{Binding Item.Price}" d:Text="Price" FontSize="Medium" Margin="0"/>
            <Label Text="Quantity" FontSize="Large" />
            <Editor Text="{Binding Item.Quantity}" d:Text="Quantity" FontSize="Medium" Margin="0"/>
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
